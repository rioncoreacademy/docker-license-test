<?php
// Copy this file to config.php and fill in the values from hPanel > Databases.
// config.php is gitignored -- never commit real credentials.

define('DB_HOST', 'localhost');
define('DB_NAME', 'u123456789_licensing');
define('DB_USER', 'u123456789_license_app');
define('DB_PASS', 'change-me');

// Protects /admin/generate and /admin/lookup (public/admin.html).
define('ADMIN_TOKEN', 'change-me-to-something-random');

// Fallback key returned by /activate and /validate for licenses with no
// product assigned (product_id IS NULL) -- see the "Products" section in
// README.md. Folder-scoped licenses use their product's own key instead.
define('DEFAULT_ENCRYPTION_KEY', 'change-me-to-a-real-encryption-key');
